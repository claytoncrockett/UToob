<h1>UToob Comments</h1>
<%= link_to  "Create Comment", new_video_comment_path(@video.id) %>
<ul>
  <% @comments.each do |comment|%>
  <li><%= link_to  comment.body, [@video, @comment] %></li>
  <%end%>
</ul>