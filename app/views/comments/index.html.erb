<h1>UToob Comment</h1>
<%= <%= link_to  "Create Comment", new_user_comment_path %>
<ul>
  <% @comments.each do |comment|%>
  <li><%= link_to  comment.name, user_comment_path(comments.id) %></li>
  <%end%>
</ul>