<h1><%= @comment.id ? 'Edit Comment' : 'Create Comment' %></h1>

<%= form_for @comment do |f| %>
<%= f.label :body %>
<%= f.text_field :body, required: true, autofocus: true %>

<%= f.submit %>
<% end %>
